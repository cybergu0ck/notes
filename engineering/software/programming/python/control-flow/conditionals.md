# Conditonal branching

<br>
<br>

## If

```py
if (condition):
    #code
```

<br>
<br>

## Else if

```py
if (conditon):
    #code
else if(conditon) :
    #code
```

<br>
<br>

## Else

```py
if (conditon):
    #code
else if(conditon) :
    #code
else :
    #code
```

- if-else conditional in one line.

  ```python
  num = 10
  is_even = True if num%2 ==0 else False
  print(is_even)

  #>True
  ```

<br>
<br>
<br>

# Switch

<br>
<br>
<br>

## pass statement

- The [`pass`](https://docs.python.org/3/reference/simple_stmts.html#pass) statement does nothing. It can be used when a statement is required syntactically but the program requires no action.

  ```py
  num = 10
  if num:
  	pass
  else:
  	print("entered else block")
  #>
  ```

<br>
<br>
